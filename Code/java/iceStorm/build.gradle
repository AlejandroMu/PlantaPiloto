def execute(dir,command,arg){
    exec{
        workingDir dir
        executable command
        args arg
    }
}
task run{
    def pid = execute("iceConfig","cmd",["/c ","icebox","--Ice.Config=config.icebox"])
}